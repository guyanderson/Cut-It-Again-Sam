@{
  Layout="Shared/_Layout.cshtml";
}


<h1>All Cuisine</h1>
@if (@Model.Count == 0)
{
  <p>There are no cuisines.</p>
}
<ul>
  @foreach (var cuisine in Model)
  {
    <li><a href="/cuisine/@cuisine.GetId()">@cuisine.GetName()</a></li>
  }
</ul>
<a href="/cuisine/new">Add new cuisine</a>
